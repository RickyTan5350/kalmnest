> Not exactly what we're going to do but referable

- Assume form
```
<form method="POST" action="/chirps">
	@csrf
	<div class="form-control w-full">
		<textarea
			name="message"
			placeholder="What's on your mind?"
			class="textarea textarea-bordered w-full resize-none"
			rows="4"
			maxlength="255"
			required
		></textarea>
	</div>

	<div class="mt-4 flex items-center justify-end">
		<button type="submit" class="btn btn-primary btn-sm">
			Chirp
		</button>
	</div>
</form>
```

- <u><b>Ignore the Tailwind CSS</b></u>
- **Summary for Form**
	- Uses POST
	- Asks for message using textarea
	- Has submit button

---
`routes/web.php`
```
use App\Http\Controllers\ChirpController;

Route::get('/', [ChirpController::class, 'index']);
Route::post('/chirps', [ChirpController::class, 'store']);
```
- `Route::post('/chirps', [ChirpController::class, 'store']);`
	- route to handle form submission
---
`ChirpController.php`
```
public function store(Request $request)
{
    // Validate the request
    $validated = $request->validate([
        'message' => 'required|string|max:255',
    ]);

    // Create the chirp (no user for now - we'll add auth later)
    \App\Models\Chirp::create([
        'message' => $validated['message'],
        'user_id' => null, // We'll add authentication in lesson 11
    ]);

    // Redirect back to the feed
    return redirect('/')->with('success', 'Chirp created!');
}
```
- Breakdown
```
$request->validate([
        'message' => 'required|string|max:255',
]);
```
- Validate if the chirp message matches requirements
	- field is required
	- input data is a string
	- maximum 255 characters
- Stores validation result in `$validated`

```
with()
```
- In this context used to temporarily store session data
	- Scope until next HTTP request only
	- known as flashing data
---
# Displaying Success Messages
```
if(session('success')) ....
```
---
# Writing Validation Messages in Validation Function
```
$request->validate([
        'message' => 'required|string|max:255',
],
[
	'message.required' => 'Please write something',
	'message.max' => "chirp must be 255 characters or less",
]);
```
